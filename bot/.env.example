DISCORD_TOKEN=your-bot-token-here
API_BASE_URL=http://localhost:4000/api
# Optional: channel id the bot proactively posts alerts to. Leave unset to disable.
ALERT_CHANNEL_ID=
# Optional: enables LLM-humanized replies. Without it, the bot uses the
# template phrasing in formatters.js (still real, still not hardcoded).
ANTHROPIC_API_KEY=
